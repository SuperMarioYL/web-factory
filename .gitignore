node_modules/
dist/

# generated by the prebuild step from site.json — never committed
src/site.generated.js
public/fonts/cjk.woff2

# a caller's site.json is copied in at build time in CI; local dev may
# symlink or copy one to the root for `npm run dev`
/site.json

# local CJK font cache (downloaded once by the prebuild)
/NotoSansSC*.ttf

.DS_Store
.claude/
CLAUDE.md

# generated per-build (host-derived) — never committed
public/robots.txt
public/sitemap.xml
